Commands:
/help shows all commands
/members shows all members
/time toggle on off timestamps
/ban or /remove from host to remove
/room change room



Implement usernames by sending username after ID in message

@ addressing for direct message

Handle multiple users attempting to start server

Multiple servers with port command line argument


Do not permit duplicate nicknames
Handle direct messageing with @s
Create multiple chat rooms by adding room# field to server's address book
    So that when someone from one room sends a message, the server checks the room of the sender and then sends it to only people in that room
